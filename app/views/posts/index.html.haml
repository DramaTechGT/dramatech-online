- content_for :sidenav, render('layouts/sidenav')
- if @category
  %h1 Posts in #{@category.name}
  .pull-right
    = link_to new_category_post_path(category_id: @category.id), class: 'btn btn-default modal-link' do
      %i.fa.fa-pencil
      &nbsp;
      New Post in #{@category.name}
- else
  %h1 Posts
  .pull-right
    = link_to new_post_path, class: 'btn btn-default modal-link' do
      %i.fa.fa-pencil
      &nbsp;
      New Post
.pull-left
  %p Create posts in our easy to use WYSIWYG editor. Yeah, you could write in Markdown or HTML, but who likes that anyway?
  %br
  #flashed-alert.text-primary= notice
- if @posts.empty?
  .row
    .col-xs-12
      %blockquote.text-primary Um, well, we don't know how to tell you this. But this is kinda sorta empty. You're gonna have to fill it with content to make it look nice and pretty... Smiles!
- else
  = smart_listing_render :posts